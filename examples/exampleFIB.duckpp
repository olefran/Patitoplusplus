%% Example FIBONACCI for Patitoplusplus
programa patito;

funcion int fibo(int i)
var int k, j;
{
	si (i == 0) entonces {
			regresa(0);
	}
	si (i == 1) entonces {
			regresa(1);
	}
	sino {
			regresa( fibo(i-1) + fibo(i-2) );
	}

}

principal()
var int x;
{
	x = fibo(5);
	escribe(x);
}
